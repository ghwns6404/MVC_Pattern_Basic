1. Server : DB구성
	//회원가입     /로그인     /로그아웃 동작 확인

2. 0424_AdminClient새로 만들어라
 비연결형으로 DataTable을 획득해라 (네트워크 쓰지마세용)
------------------------------------------------------------------------------
다른서버 만들고 할 일 (에코가 되는 서버를 만들어야함)
  1>> 채팅 서버 : 메세지 echo -> 1:N
  2 >>채팅창이 Load될때 채팅서버에 연결
  3 >>채팅창이 Closed될때 채팅서버에서 연결 해제 
------------------------------------------------------------------------------
   3. 관리자 클라에 네트워크 연결
     >>서버에 회원가입/로그아웃/로그인 패킷 (1:N 통신구조 )
     >>> 메세지 수신 후 화면 갱신 (5초마다, Timer)
------------------------------------------------------------------------------

@@클라에서 보낼때 color.name으로 보내야함

@클라 : MemberControl에서 AddMember함수에서 color 넘길때 color.name으로 넘기세요

//DB에 저장(insert) : id(PK), islog(bool), pw, name, color(string or int), date,
